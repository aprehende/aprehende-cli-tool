{{#if withAs}}
  {{#if withHook}}
export { default as useHello } from './useHello';
  {{else}}
export { default as {{hookName}} } from './{{hookName}}';
  {{/if}}
{{else}}
  {{#if withHook}}
export { default } from './useHello';
  {{else}}
export { default } from './{{hookName}}';
  {{/if}}
{{/if}}
