import React from 'react';

interface {{hookName}}Props {}
const {{hookName}} = ({}: {{hookName}}Props) => {
  const greet = () => {
  console.log('Hello, world!');
  };

  return { greet };
};

export default {{hookName}};
