import React from 'react';

type useHelloHook = (name: string) => string;

const useHello = (): useHelloHook => {
  const greet: useHelloHook = (name): string => {
    return `Hello ${name}`;
  };

  return { greet };
};

export default useHello;
